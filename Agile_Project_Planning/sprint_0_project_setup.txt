================================================================================
   PARKIPAY — SPRINT 0: Project Setup, Infrastructure & CI/CD
================================================================================
Duration : Week 1 (5 working days)
Status   : ✅ COMPLETE
================================================================================

SPRINT GOAL
"All developers can clone the repo, run the backend with npm run dev, and the
API is available on port 8000. A push to develop runs all CI checks and
auto-deploys to Render on merge to main."

SUCCESS CRITERIA
  ✅ GET /api/health/ → { "status": "ok", "database": "ok" }
  ✅ npm run db:seed creates ADMIN001 and OFF001
  ✅ npm test passes with 11 tests green
  ✅ npm run lint passes with 0 errors
  ✅ docker-compose up starts API + Postgres (for non-Termux machines)
  ✅ backend-ci.yml: lint → audit → jest → auto-merge to main
  ✅ backend-cd.yml: Render deploy hook on main push
  ✅ Prisma schema has all 6 tables with correct indexes
  ✅ .env.example documents every variable

DELIVERABLES
  backend/src/app.js                  Express app + Helmet/CORS/rate-limit
  backend/src/server.js               DB connect check + graceful shutdown
  backend/src/config/index.js         Central env config
  backend/src/lib/prisma.js           Singleton PrismaClient
  backend/src/lib/jwt.js              signAccess/signRefresh/blacklist
  backend/src/lib/audit.js            Audit log helper (swallows errors)
  backend/src/lib/controlNumber.js    CN generation + duplicate check query
  backend/src/middleware/auth.js      authenticate + requireRole
  backend/src/middleware/errorHandler.js
  backend/src/routes/health.js        GET /api/health/ with DB ping
  backend/src/routes/auth.js          Full auth implementation
  backend/src/routes/vehicles.js      lookup + locations
  backend/src/routes/billing.js       generate + history + status
  backend/prisma/schema.prisma        6 tables, enums, indexes
  backend/prisma/seed.js              ADMIN001 + OFF001 + sample data
  backend/__tests__/auth.test.js      Jest integration tests (11 cases)
  backend/Dockerfile                  Multi-stage Node 20 Alpine
  backend/docker-compose.yml          API + local PostgreSQL 16
  backend/entrypoint.sh               migrate → node server.js
  .github/workflows/backend-ci.yml    CI pipeline
  .github/workflows/backend-cd.yml    CD pipeline

USER STORIES
  US-000: Repo structure
    AC: Clean folder structure, .gitignore, .env.example, README present
  US-001: Local backend runs
    AC: npm run dev → "✅ Database connected. 🚀 ParkiPay API running"
  US-002: DB schema deployed
    AC: npm run db:migrate runs with 0 errors
  US-003: Test data seeded
    AC: npm run db:seed → ADMIN001/Admin@1234, OFF001/Officer@1234
  US-004: Health endpoint
    AC: GET /api/health/ → 200 { status: "ok", database: "ok" }
  US-005: CI on push
    AC: Push to develop → lint + audit + tests run automatically
  US-006: CD on merge to main
    AC: Merge to main → Render deploy hook triggered → new version live
