"""ParkiPay — Billing URL Routes (Sprint 3)"""
urlpatterns = [
    # Sprint 3: control number generation & history
    # path("generate/", GenerateBillView.as_view(), name="billing-generate"),
    # path("history/", BillingHistoryView.as_view(), name="billing-history"),
    # path("<str:control_number>/status/", BillStatusView.as_view(), name="bill-status"),
]
